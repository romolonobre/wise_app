/// RecentCurrency Model that we use
/// to populate the currency card

class RecentCurrencyModel {
  final String imageFlag;
  final String amount;
  final String currencyType;

  RecentCurrencyModel({
    required this.imageFlag,
    required this.amount,
    required this.currencyType,
  });
}
