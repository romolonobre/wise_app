import 'package:flutter/material.dart';

class RecipientPages extends StatelessWidget {
  const RecipientPages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('recipient'),
      ),
      body: Container(),
    );
  }
}
